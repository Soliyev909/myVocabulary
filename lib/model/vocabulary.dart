import 'package:my_vocabulary/model/word_model.dart';

class Vocabulary{
  static List<Word> words = [word1,word2];
}

Word word1 = Word(1, {'Hello':'Salom'},DateTime.april.toString());
Word word2 = Word(2, {'name': 'ism'},DateTime.july.toString());
